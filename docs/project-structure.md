# Project File Structure - My Piano Diary

This document provides an overview of the file structure and organization of the **My Piano Diary** project.

## 📂 Root Directory

The root directory contains configuration files for the development environment, build tools, and dependencies.

- `prisma/`: Database schema and migrations.
- `public/`: Static assets like images and fonts.
- `src/`: The main source code of the application.
- `docs/`: Project documentation.
- `.env`: Environment variables (local).
- `components.json`: Shadcn/UI configuration.
- `next.config.js`: Next.js configuration.
- `package.json`: Project dependencies and scripts.
- `tsconfig.json`: TypeScript configuration.

---

## 📂 `src/` Directory

The core logic and UI of the application reside here.

### 📍 `app/` (Next.js App Router)

Handles routing, layouts, and pages.

- `(auth)/`: Authentication-related routes (Login, Register).
- `(root)/`: Main landing or restricted root pages.
- `api/`: Route handlers (e.g., tRPC API endpoint).
- `dashboard/`: User dashboard and management pages.
- `layout.tsx`: Root layout shared across the app.
- `page.tsx`: The main landing page.

### 📍 `components/`

Reusable UI components.

- `ui/`: Lower-level, primitive components (mostly from Shadcn).
- Shared components like `NavUser`, `SiteHeader`, etc.

### 📍 `server/`

Server-side logic and database interactions.

- `api/`: tRPC router definitions and procedures.
- `auth/`: NextAuth configuration and providers.
- `db.ts`: Prisma client initialization.
- `actions/`: Server actions for form handling and logic.

### 📍 `trpc/`

tRPC client-side configuration and provider setup for React Query integration.

### 📍 `store/`

Global state management (Zustand).

### 📍 `hooks/`

Custom React hooks for shared logic.

### 📍 `lib/`

Utility functions and shared constants (e.g., `utils.ts` for Tailwind merge).

### 📍 `styles/`

Global CSS files and Tailwind configuration.

---

## 📂 `prisma/`

- `schema.prisma`: The source of truth for the database schema.
- `migrations/`: SQL migration files generated by Prisma.

---

## 🛠 Tech Stack

- **Framework**: Next.js (App Router)
- **Language**: TypeScript
- **Database**: Prisma ORM with Neon (PostgreSQL)
- **Auth**: NextAuth.js
- **API**: tRPC
- **State**: Zustand
- **Styling**: Tailwind CSS & Shadcn/UI
- **Icons**: Lucide React
